# Раздел настроек плитки: что было в прошлых версиях и что стало

Дата: 18 сентября 2026.

## Что нашлось в истории репозитория (файлы-доноры)

| Донор | Что там было | Что взято |
| --- | --- | --- |
| `a217e7e^:src/presets.css` | сетка `.preset-grid` в три столбца и **девять** именованных видов: soft, compact, flat, neon, glass, neumorphic, layered, aurora, elevated | сама идея девяти видов и их имена |
| `a217e7e^:src/components/SettingsPanel.tsx` | выбор пресета вызывал `setTile({...tile, preset})` и менял только плотность сетки | **не взято**: оформление плитки от пресета не зависело, картинка в кнопке была нарисована вручную в CSS — ровно тот случай «кнопка без функции», который сейчас запрещён |
| `6fd0822^:src/domain/tilePresets.ts` | пресет как полный объект настроек + `normalizeTileSettings` с границами | структура «пресет = полный набор значений» и клэмп по границам |
| `6fd0822^:src/state/tileStyle.ts` | перевод настроек в CSS-переменные `--tile-*` | механизм целиком |
| `6fd0822^:src/domain/tileInteraction.ts` | состояния плитки: normal, hover, pressed, selected, focus-visible, dragging, drop-target | перечень состояний, которые должны иметь настройку |
| `6fd0822^:src/components/settings/TilePreview.module.css` | полоса предпросмотра с классами `forceHover` и `forcePressed` | предпросмотр трёх состояний |
| `6fd0822^:src/components/tiles/SiteTile.module.css` | правила плитки, целиком собранные из переменных | образец того, как переменные доходят до пикселей |

Итог разбора: в старом поколении было **девять** видов, но они ничего не меняли;
в более новом поколении механика была настоящей, но видов было **пять** и они
меняли только геометрию. Новый раздел соединяет одно с другим.

## Как устроено сейчас

`src/domain/tileAppearance.ts` — единственный источник правды:

* 36 параметров: `preset`, `mode` и 34 отдельных значения в четырёх группах: размер и сетка, поверхность, реакция на
  взаимодействие, содержимое;
* `TILE_BOUNDS` задаёт границы и шаг каждого числового параметра;
* `TILE_PRESETS` — девять полных наборов; каждый меняет подложку, тень и реакцию,
  а не только подпись под кнопкой;
* `toTileVars()` переводит набор в CSS-переменные, которые читает `.nx-tile`
  в `src/app/theme.css`.

Раздел «Плитки» (`src/app/TileSettings.tsx`):

1. **Готовый вид** — сетка 3×3. В каждой карточке настоящая плитка, уменьшенная
   до 0.38, с переменными своего пресета. Нарисованных вручную образцов нет.
2. **Как плитка отвечает на действия** — тот же сайт в трёх состояниях:
   обычная, наведение, нажатие. Образцы используют те же переменные.
3. **Размер и сетка** (9 контролов), **Поверхность** (12), **Реакция на
   взаимодействие** (9), **Что показывать на плитке** (6) — сетка ровно в три
   столбца, число ячеек в каждой группе кратно трём, у каждой своя иконка.

## Против фейковых контролов

* `src/domain/tileAppearance.test.ts` перебирает **каждое** поле настроек и
  падает, если изменение поля не меняет результат `toTileVars`.
* e2e «ни один контрол раздела «Плитки» не остаётся без действия» в браузере
  двигает каждый ползунок, переключает каждый список и каждый тумблер и сверяет
  отпечаток вычисленных стилей настоящей плитки на главной странице.
* Контрол, который на текущих значениях ничего не изменит (размытие на плотной
  подложке, глубина тени при выключенной тени), гасится и пишет причину рядом —
  вместо того чтобы молча ничего не делать.

Эта проверка нашла настоящий мёртвый контрол: переключатель «Значки уведомлений».
Поле `badge` не заполнял ни один путь в приложении — ни форма добавления сайта,
ни импорт закладок, ни мост расширения, — то есть значок не мог появиться
никогда. Значок удалён вместе с полем и переключателем, на его место в группе
«Что показывать на плитке» встала **категория сайта**, которая в записи есть
всегда.


## Правка от 18 сентября: без неона, размытия и устаревших приёмов

По требованию «полностью исключить неон, размытость и старый неактуальный
дизайн» из модели убрано:

* **вид «Неон»** вместе с кольцевым свечением (`shadowStyle: 'ring'`) и
  параметром «Свечение» (`hoverGlow`);
* **вся механика размытия**: параметры «Размытие фона», «Насыщенность» и
  «Непрозрачность», подложка `translucent`, переменная `--nx-tile-blur` и
  все `backdrop-filter` в листах стилей, включая затемнение под окнами;
* **устаревшие тени**: неоморфная (пик моды 2020 года) и слоистая из эпохи
  плоского дизайна;
* **вид «Стекло»** — он держался на размытии и исчез вместе с ним;
* **`src/styles.css`** целиком: это был предыдущий макет приложения —
  боковая колонка, карточки сайтов, док, окно настроек — с жёстко
  прописанными цветами и `backdrop-filter: blur(22px)`. Из 80 классов листа
  живые окна использовали 26, и почти все перекрывались текущей темой.
  Нужные 2 КБ правил перенесены в `theme.css` на общие токены; лист стилей
  приложения уменьшился с 67 до 54 КБ (13,5 → 10,2 КБ в gzip).

Добавлено взамен:

* параметр **«Тень под курсором»** — во сколько раз тень усиливается при
  наведении; он занял место «Свечения», и группа осталась кратной трём;
* тип тени **«Мягкая»** — три слабых слоя вместо одного жёсткого;
* группа «Поверхность» сократилась с 12 контролов до 9.

Девять видов теперь такие: Мягкий, Плотный, Плоский, Контур, Парящий,
Аврора, Песочный, Тёмный, Акцент. Ни один не использует размытия и свечения;
тест `нигде не осталось размытия, свечения и неонового вида` перебирает все
девять и падает, если в значениях или переменных встретится `blur`,
`saturate`, `neon`, `glass`, `translucent`, `neumorphic`, `layered`,
`elevated` или `glow`. Отдельный тест проверяет, что профиль, сохранённый
прежней версией приложения, молча заменяется на действующие значения.
