import { useState } from 'react';
import { X } from 'lucide-react';
import type { SiteRecord as Site } from '../domain/types';

export function AddSiteModal({onClose,onSave,existing,categories}:{onClose:()=>void;onSave:(site:Site)=>void;existing?:Site;categories:string[]}){const [title,setTitle]=useState(existing?.title||'');const [domain,setDomain]=useState(existing?.domain||'');const [desc,setDesc]=useState(existing?.desc||'');const [category,setCategory]=useState(existing?.category||categories[0]||'Личное');const [note,setNote]=useState(existing?.note||'');const [color,setColor]=useState(existing?.color||'#2f7cf6');const submit=(e:React.FormEvent)=>{e.preventDefault();const clean=domain.replace(/^https?:\/\//,'').split('/')[0];if(!title.trim()||!clean)return;onSave({...existing,title:title.trim(),domain:clean,desc:desc.trim()||'Сохранённый сайт',note:note.trim(),color,icon:title.trim()[0].toUpperCase(),category})};return <div className="overlay"><form className="site-form glass" onSubmit={submit}><header><div><span className="eyebrow">БЫСТРЫЙ ДОСТУП</span><h2>{existing?'Редактировать сайт':'Добавить сайт'}</h2><p>Сохраните любимый сайт в одну плитку</p></div><button type="button" className="close" onClick={onClose}><X/></button></header><label>Название<input autoFocus value={title} onChange={e=>setTitle(e.target.value)} placeholder="Например, Linear"/></label><label>Адрес сайта<input value={domain} onChange={e=>setDomain(e.target.value)} placeholder="linear.app"/></label><label>Категория<select value={category} onChange={e=>setCategory(e.target.value)}>{categories.map(c=><option key={c}>{c}</option>)}</select></label><label>Описание<input value={desc} onChange={e=>setDesc(e.target.value)} placeholder="Короткое описание"/></label><label>Заметка<input value={note} onChange={e=>setNote(e.target.value)} placeholder="Личная заметка (необязательно)"/></label><label>Цвет иконки<div className="form-colors">{['#2f7cf6','#885cf6','#e95674','#ef9e35','#22a87c','#1c9dbb'].map(c=><button type="button" key={c} className={color===c?'active':''} style={{background:c}} onClick={()=>setColor(c)}/>)}</div></label><footer><button type="button" className="reset" onClick={onClose}>Отмена</button><button className="save" type="submit" disabled={!title.trim()||!domain.trim()}>{existing?'Сохранить':'Добавить сайт'}</button></footer></form></div>}
