import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import {resolve} from 'node:path';import {normalizeSiteUrl} from '../components/sites/siteEditorModel.ts';
const read=(path:string)=>readFile(resolve(process.cwd(),path),'utf8');
test('normalizes user URL safely',()=>{assert.deepEqual(normalizeSiteUrl('openai.com'),{url:'https://openai.com/',domain:'openai.com'});assert.equal(normalizeSiteUrl('javascript:alert(1)'),null);assert.equal(normalizeSiteUrl('data:text/html,x'),null);});
test('grid and compact site editor use persistent store boundary',async()=>{const[grid,tile,editor,css,app]=await Promise.all([read('src/components/tiles/SpeedDialGrid.tsx'),read('src/components/tiles/SiteTile.tsx'),read('src/components/sites/SiteEditor.tsx'),read('src/components/sites/SiteEditor.module.css'),read('src/App.tsx')]);assert.ok(!grid.includes('seedSites'));assert.ok(grid.includes("setSiteEditor('new')"));assert.ok(tile.includes('setSiteEditor(site.id)'));for(const action of['addSite','updateSite','removeSite'])assert.ok(editor.includes(action));assert.ok(!editor.includes('localStorage'));assert.match(css,/position\s*:\s*fixed/);assert.ok(app.includes('<SiteEditor'));});
